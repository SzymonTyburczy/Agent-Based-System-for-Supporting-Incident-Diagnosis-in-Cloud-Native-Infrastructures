# Diagnostic agent (agent-core/webhook_server.py)
VITE_AGENT_API_URL=http://localhost:8090
VITE_AGENT_API_TOKEN=

# Local document converter (doc-converter). No model key here — it belongs to
# the service's own .env; see doc-converter/README.md.
VITE_CONVERTER_URL=http://localhost:5001
VITE_CONVERTER_TOKEN=
